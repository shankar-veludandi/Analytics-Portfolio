# .env.template (safe to commit)
ENV=dev

# RapidAPI credential (shared for Realtor + Redfin)
API_KEY=YOUR_RAPIDAPI_KEY

# Database (choose ONE)
# A) Single URL (preferred)
DATABASE_URL=postgresql+psycopg2://postgres:password@host:5432/postgres

# B) Parts
DB_HOST=host.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_password
